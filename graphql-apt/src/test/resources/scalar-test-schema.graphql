"An RFC-3339 compliant DateTime Scalar"
scalar DateTime

type Query {
  battle(id: ID!): Battle
  battles: [Battle]
}

type Battle {
  id: ID!
  name: String!
  startTime: DateTime!
  endTime: DateTime
}
